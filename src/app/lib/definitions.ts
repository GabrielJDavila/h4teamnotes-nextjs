export type Clients = {
    id: string,
    firstname: string,
    lastname: string,
    weight: string,
    age: string,
    note: string
}

export type RecentNotes = {
    tablename: string,
    id: string,
    username: string,
    date: string,
    note: string
}

export type Notes = {
    id: string,
    username: string,
    date: string,
    note: string
}

export type GymNotes = {
    id: string,
    username: string,
    date: string,
    note: string
}